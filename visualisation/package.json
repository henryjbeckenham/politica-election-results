{
  "name": "politica-election-results-public-site",
  "private": true,
  "version": "1.8.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/clean-build.mjs && observable build",
    "build:app": "npm run build && node scripts/stage-build.mjs",
    "preview": "observable preview --host 127.0.0.1",
    "test": "node --test tests/results.test.mjs"
  },
  "dependencies": {
    "@observablehq/framework": "1.13.4"
  },
  "devDependencies": {
    "jsdom": "23.2.0"
  }
}
