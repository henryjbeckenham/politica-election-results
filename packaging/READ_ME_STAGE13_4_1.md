# Stage 13.4.1 v1.3.5 corrective update

Run `install_stage13_4_1.command` from the extracted update folder. The installer accepts Stage 13.4 or a repeat Stage 13.4.1 installation, creates a Desktop backup, installs the practical deep-zoom map and electorate finder, runs the complete Stage 4–13.4.1 regression suite, validates the unchanged election release and builds a new host-ready static website ZIP.

Node.js is not required on the operator's Mac. The tested public site is already compiled. Nothing is uploaded or externally deployed.
